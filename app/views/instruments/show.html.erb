<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @instrument.name %>
</p>
<p>
  <strong>Instructors:</strong>
  <% @instrument.instructors.each do |instructor| %>
  <%= instructor.name %>
  <% end %>
</p>

<%= link_to 'Edit', edit_instrument_path(@instrument) %> |
<%= link_to 'Back', instruments_path %>
